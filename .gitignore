# Build artifacts
/target

# Personal save files used for local integration testing — never committed.
# See CONTRIBUTING.md and the PTPNHEX_SAVES_DIR environment variable.
SAVEDATA/

# Backup files produced by the editor
*.bak

# Local working data — decrypted save corpus, key material, card backups. Never committed.
.local-keys/

# Scratch save fixtures for local integration testing.
test-saves/

# macOS Finder metadata.
.DS_Store
